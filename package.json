{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "start-dev": "lerna run start --parallel",
    "start-prod": "lerna run start --parallel --scope local-server --scope cli"
  }
}
